# Chimera

A simple app for exploring selected concepts related to Spring Boot.

## How to use this

1. To format code use `spotlessApply` task.
   ```bash
   ./gradlew spotlessApply
   ```
2. To build project use `build` task.
   ```bash
   ./gradlew build
   ```
3. For doing all-in-one, declare all tasks at once.
   ```bash
   ./gradlew clean spotlessApply build
   ```
